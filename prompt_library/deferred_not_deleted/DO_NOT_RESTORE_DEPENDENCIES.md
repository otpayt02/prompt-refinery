# Do Not Restore Dependencies Here

This folder is for prompt-library recovery only.

Do not place generated dependency folders here:

```text
.venv/
node_modules/
dist/
__pycache__/
```

If a dependency environment is needed, recreate it locally from setup commands or dependency manifests. Keep generated environments out of Git.
