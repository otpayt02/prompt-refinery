# Merge Note

This folder resolves the concern that prompt-library material looked deleted during cleanup.

It keeps known legacy prompt copies inside the underscore `prompt_library/` tree while preserving the categorized canonical templates.
