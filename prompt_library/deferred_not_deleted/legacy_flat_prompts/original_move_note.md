# Original Move Note

The original migration target for prompt assets is:

```text
prompt_library/
```

The hyphenated folder name:

```text
prompt-library/
```

is treated as staging/import only.

This folder preserves legacy flat prompt copies so the organized migration does not look like silent deletion.
